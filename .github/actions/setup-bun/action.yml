name: "Setup Bun"
description: "Set up Bun and install frozen workspace dependencies"
inputs:
  install-flags:
    description: "Additional flags to pass to bun install"
    required: false
    default: ""
runs:
  using: "composite"
  steps:
    - name: Setup Node
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
      with:
        node-version: "24"

    - name: Setup Bun
      uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
      with:
        # Keep CI deterministic and avoid composite-action path ambiguity when
        # setup-bun tries to resolve package.json as a version file.
        bun-version: "1.3.14"

    - name: Verify Bun version
      run: |
        actual="$(bun --version)"
        if [ "$actual" != "1.3.14" ]; then
          echo "Expected Bun 1.3.14, got $actual" >&2
          exit 1
        fi
      shell: bash

    - name: Install setuptools for distutils compatibility
      run: python3 -m pip install setuptools || pip install setuptools || true
      shell: bash

    - name: Install dependencies
      run: |
        if [ "$RUNNER_OS" = "Windows" ]; then
          bun install --frozen-lockfile --linker hoisted ${{ inputs.install-flags }}
        else
          bun install --frozen-lockfile ${{ inputs.install-flags }}
        fi
      shell: bash
